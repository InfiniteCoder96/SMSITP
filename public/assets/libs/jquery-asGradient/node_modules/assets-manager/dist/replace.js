'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

exports.default = function (content, rules) {
  var keys = Object.keys(rules);

  keys.forEach(function (search) {
    var replacement = rules[search];

    var regex = (0, _toRegExp2.default)(search);
    if (regex) {
      content = content.replace(regex, replacement);
    } else {
      content = content.split(search).join(replacement);
    }
  });

  return content;
};

var _toRegExp = require('./toRegExp');

var _toRegExp2 = _interopRequireDefault(_toRegExp);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

module.exports = exports['default'];